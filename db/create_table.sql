CREATE TABLE users 
(id SERIAL PRIMARY KEY, img text, first_name text, last_name text, gender text, hobby text, hair text, eye text, birthday_year integer, birthday_month integer, birthday_day integer) 